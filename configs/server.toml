# RustProxy 服务端配置

[server]
bind_addr = "0.0.0.0"
bind_port = 7000
# 客户端认证 Token，客户端必须携带相同 Token 才能连接
token = "your-secret-token"
# HTTP 代理监听端口（0 = 不监听，多个 HTTP 代理规则共用此端口，基于域名路由）
http_port = 8080
# HTTPS 代理监听端口（0 = 不监听，多个 HTTPS 代理规则共用此端口，基于 SNI 域名路由）
https_port = 8443

[web]
enable = true
bind_addr = "0.0.0.0"
bind_port = 7500
user = "admin"
password = "admin"

[tls]
# 自动生成自签证书（首次启动后保存到 cert_file/key_file 指定路径）
auto_cert = true
# 证书和密钥文件路径（auto_cert=true 时自动生成保存）
cert_file = "certs/server.crt"
key_file = "certs/server.key"

# 数据库文件路径（默认为与配置文件同目录下的 rustproxy.db）
# 启动时可通过 --db 参数覆盖
# 如: rustproxy-server --db /var/lib/rustproxy/rustproxy.db
