# RustProxy 客户端配置
# 客户端无需配置代理规则，所有规则由服务端 Web 面板管理
# 客户端每次启动自动从服务端拉取属于自己的代理规则

[client]
# 客户端唯一标识（服务端通过此 ID 关联代理规则）
id = "my-laptop"
# 服务端地址
server_addr = "127.0.0.1"
server_port = 7000
# 必须与服务端 token 一致
token = "your-secret-token"
# 服务端 CA 证书路径，留空则信任自签证书（auto_cert 模式）
ca_cert = ""
